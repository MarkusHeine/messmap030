import React, { MouseEvent } from "react";
import axios from "axios";

const App: React.FC = () => {
    return (
        <div className="App">
            <p>app is running</p>
        </div>
    );
};

export default App;
